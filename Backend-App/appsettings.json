{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "ImportData": false, // This flag is use when frst time create database and import data from files to Postgre database.
  "ConnectionStrings": {
    "PostgreContext": "Host=localhost;Database=GisAplikacija;Username=postgres;Password=nemasifra;Pooling=false;Timeout=300;CommandTimeout=300"
  }
}
